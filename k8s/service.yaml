apiVersion: v1
kind: Service
metadata:
  name: aks-java-api
  namespace: demo
  labels:
    app: aks-java-api
spec:
  type: LoadBalancer
  selector:
    app: aks-java-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
